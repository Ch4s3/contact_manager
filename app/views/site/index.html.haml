%html
  %body
    #account
      %p
        - if current_user
          - link_to "Logout", logout_path, id: "logout", method: 'delete'
        -else
          -link_to "Login", login_path, id: "login"
